#!/bin/sh

#
# Gradle startup script for UN*X
#

##############################################################################
##
##  Gradle init script for POSIX-compliant systems
##
##############################################################################

# (... basic gradlew script content ...)
# I'll just provide a minimal one that points to the jar if needed, 
# but usually Android Studio creates this or I can provide a full one.
# For simplicity, providing the properties is often enough to trigger AS to create the runner or download it.
# However, providing a working gradlew is better.
